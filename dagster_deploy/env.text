# Environment variable key-value pairs that will be loaded into the environment locally when
# `dagster dev` is run in the same folder as this file.
DATABRICKS_PROJECT_ENV=dev
DATABRICKS_SCHEMA=dbt-nstankus